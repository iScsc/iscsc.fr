FROM nginx:1.23.2

ARG NODE_PORT
ENV NODE_PORT=$NODE_PORT

ARG REACT_PORT
ENV REACT_PORT=$REACT_PORT

ARG SERVER_NAME
ENV SERVER_NAME=$SERVER_NAME

WORKDIR /nginx
COPY . .
CMD ./run_nginx.sh
EXPOSE 80
